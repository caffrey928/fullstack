{
  "name": "backend",
  "version": "1.0.0",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn prisma generate && yarn prisma migrate deploy && node --experimental-specifier-resolution=node src/index.js",
    "dev": "nodemon --experimental-specifier-resolution=node src/index.js"
  },
  "dependencies": {
    "@prisma/client": "4.11.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "csrf-csrf": "^2.2.3",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "glob": "^9.2.1",
    "memorystore": "^1.6.7",
    "prisma": "^4.11.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.21"
  }
}
